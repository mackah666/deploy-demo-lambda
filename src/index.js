exports.handler = async (event) => {
    // TODO implement
    const response = {
        statusCode: 200,
        body: JSON.stringify('Hello Awaze from Manchester, once again'),
    };
    return response;
};
